[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hist-smooth"
version = "0.1.1"
description = "A library for smoothing histograms."
readme = "README.md"

authors = [
  { name = "Roman", email = "petrov.romash@gmail.com" },
]
requires-python = ">=3.9"

dependencies = [
  "hist[plot,fit]>=2.8.0",
]

[project.urls]
Homepage = "https://github.com/pingmar/smoothing_algorithms"

[tool.hatch.build]
sources = ["src"]